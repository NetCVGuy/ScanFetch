# Quick Start Guide - ScanFetch Development

## 🚀 Первый запуск / First Run

### 1. Настройка окружения
```bash
cd /run/media/zahar/moding/VSPRJ/ScanFetch
dotnet restore ScanFetchDotNet.sln
```

### 2. Сборка проектов
```bash
# Сборка основного приложения
./build-linux.sh

# Сборка эмулятора сканера
./build-testscanner.sh
```

### 3. Запуск в режиме разработки

#### Терминал 1: TestScanner (сервер - эмулирует сканер)
```bash
dotnet run --project TestScanner/TestScanner.csproj
# Выберите: Server mode
# Port: 2002
```

#### Терминал 2: ScanFetch (клиент - подключается к эмулятору)
```bash
# Убедитесь, что в appsettings.json сканер настроен в режиме Client:
# "Role": "Client", "Ip": "127.0.0.1", "Port": 2002

dotnet run --project ScanFetch.csproj
```

## 🐛 Отладка / Debugging

### VS Code
1. Откройте проект в VS Code
2. **F5** для запуска ScanFetch с отладчиком
3. Или **Ctrl+Shift+P** → "Debug: Select and Start Debugging" → Выбрать конфигурацию

### Точки останова
- Установите breakpoint в любом .cs файле
- Отладчик остановится на точке останова
- Используйте Debug Console для выполнения команд

## 📋 Полезные команды

### Проверка ошибок компиляции
```bash
dotnet build ScanFetch.csproj
```

### Очистка кэша сборки
```bash
rm -rf obj bin TestScanner/obj TestScanner/bin
```

### Просмотр логов в реальном времени
```bash
tail -f Logs/$(ls -t Logs/ | head -1)
```

### Тестирование Google Sheets вебхука
```bash
curl -X POST https://YOUR_WEBHOOK_URL \
  -H "Content-Type: application/json" \
  -d '{"code":"TEST123","scanner":"TestScanner","remote":"127.0.0.1:12345"}'
```

## 🔧 Настройка для вашего сканера

### Примеры конфигураций

Проект включает готовые примеры конфигураций:

**Для работы с TestScanner (эмулятор):**
```bash
cp appsettings.testscanner.json appsettings.json
```

**Для Hikrobot ID2000 (реальный сканер):**
```bash
cp appsettings.hikrobot.json appsettings.json
# Отредактируйте ListenInterface под ваш сетевой интерфейс
```

### Конфигурация appsettings.json

**Для TestScanner (Client mode):**
```json
{
  "Scanners": [
    {
      "Name": "TestScanner",
      "Ip": "127.0.0.1",
      "Port": 2002,
      "Role": "Client",
      "TimeoutFlushMs": 50,
      "RequestIntervalMs": 100
    }
  ]
}
```

**Для Hikrobot ID2000 (Server mode):**
```json
{
  "Scanners": [
    {
      "Name": "Hikrobot_ID2000",
      "Ip": "",
      "Port": 2002,
      "Role": "Server",
      "TimeoutFlushMs": 50,
      "RequestIntervalMs": 100,
      "ListenInterface": "eth0"  // или IP вашего интерфейса
    }
  ]
}
```

### Важные отличия режимов:

**Client Mode (для TestScanner):**
- ScanFetch подключается к указанному `Ip:Port`
- Не требует выбора сетевого интерфейса
- Используется для тестирования с эмулятором

**Server Mode (для реальных сканеров):**
- ScanFetch слушает на `Port`, сканер подключается к нему
- **Требует выбора сетевого интерфейса** при наличии нескольких
- Установите `ListenInterface` для автоматического выбора
- Используется для Hikrobot ID2000 и подобных сканеров

### Подключение Hikrobot ID2000

1. Настройте сканер на подключение к IP вашего ПК
2. Порт: 2002 (или другой, указанный в конфиге)
3. Настройте `appsettings.json` в Server mode:
   ```bash
   cp appsettings.hikrobot.json appsettings.json
   ```
4. Установите `ListenInterface` если у вас несколько сетевых интерфейсов
5. Запустите ScanFetch - сканер автоматически подключится

## 📦 Сборка для продакшена

### Linux
```bash
./build-linux.sh
# Результат: publish/linux-x64/ScanFetch
```

### Windows (на Linux через кросс-компиляцию)
```bash
dotnet publish ScanFetch.csproj \
  -c Release \
  -r win-x64 \
  --self-contained true \
  -p:PublishSingleFile=true \
  -o publish/win-x64
# Результат: publish/win-x64/ScanFetch.exe
```

## 🆘 Решение проблем

### "Multiple network interfaces found"
- Появляется только в Server mode (когда ScanFetch слушает порт)
- Установите `ListenInterface` в `appsettings.json` для автоматического выбора
- Или выберите интерфейс в консоли при первом запуске

### "Connection refused"
- Проверьте, что порт не занят: `sudo netstat -tlnp | grep 2002`
- Проверьте firewall: `sudo ufw status`

### TestScanner не может подключиться
- Убедитесь, что ScanFetch запущен в Server mode
- Проверьте IP и порт в конфигурации
- Проверьте, что порт не блокируется firewall

## 📚 Дополнительная информация

- **README.md** - Полная документация проекта
- **.github/copilot-instructions.md** - Инструкции для AI ассистентов
- **TestScanner/README.md** - Документация эмулятора сканера
- **scripts/google_sheets_script.js** - Скрипт для Google Sheets

## 🎯 Типичный рабочий процесс

1. Изменение кода → `dotnet build` → проверка ошибок
2. Тестирование → Запуск ScanFetch + TestScanner
3. Отладка → F5 в VS Code, установка breakpoint'ов
4. Коммит изменений → `git add . && git commit -m "..."`
5. Сборка релиза → `./build-linux.sh` или `build-windows.bat`
